<UserControl x:Class="UWUVCI_AIO_WPF.UI.Controls.SourceButtonsControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" >
    
    <WrapPanel >
        <Button Style="{DynamicResource CustomButtonStyle}" Width="120" VerticalAlignment="Center" Height="40"
            Command="{Binding OpenDialogCommand}">Choose Source File</Button>
        <Button Margin="10,0" Style="{DynamicResource CustomButtonStyle}" Width="100" VerticalAlignment="Center" Height="40"
            Command="{Binding ClearProcessedFileCommand}">Clear Source</Button>
    </WrapPanel>
</UserControl>
